import {
  StyleSheet,
} from 'react-native';

export const myStyles = StyleSheet.create({
  topTabText: {
    fontSize: 10,
    textAlign: 'center',
    color: 'black',
    paddingTop: 2,
  },
  textBold: {
    fontWeight: 'bold',
  },
  kb: {
    flex: 1,
    justifyContent: 'space-between',
  },
});